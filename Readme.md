### Summary 
主要是将onnx转为使用英特尔显卡GPU推理用的格式， 共生成2个文件， 一个.bin, 一个.xml文件， 还有加密功能

### Note
因为onnx模型中已经集成了数据预处理，  所以在推理时不需要做额外的处理了
if self.export_mode:
    x = x / torch.max(x)          # 近似 /255
    x = self.img_normalize(x)     # (x - 0.5) / 0.5

关于模型加密：  主要是要进行密码管理， 常见的就是将其写入环境变量中， 而我要考虑的问题是在打包成软件之后， 怎么直接将密码写入到部署的电脑环境变量中
